# PostgreSQL connection
DATABASE_URL=postgres://user:password@localhost:5432/bcs_tickets

# JWT secret for staff authentication
JWT_SECRET=change-this-secret

# Optional: secret required to allow creating initial staff via /api/auth/register
REGISTER_SECRET=change-me

# Server port
PORT=3000
